# 项目记忆：AI Agent 学习项目

## 项目定位

- 项目所有者是后端开发者 liter，当前核心目标不是尽快堆出功能，而是系统学习 AI 与 Agent 应用开发，形成可迁移的 AI 工程能力。
- 本项目采用“概念理解 → 最小实验 → 指标验证 → 工程化”的学习路径。
- 功能已经运行不等于已经掌握；只有能解释原理、设计对照实验、分析失败案例并通过测试或评测，才把能力标记为已掌握。
- UI 和普通 CRUD 只服务于 AI 实验。除非它们阻塞实验，否则不要让前端美化或常规后端重构挤占 AI 学习优先级。

## 当前项目结构约定

- `app.py`：FastAPI 接口、Agent 调用入口和消息序列化。
- `state.py`：自定义 Agent State、`Command` 状态更新和最小短期记忆示例。
- `store.py`：模型、工具、短期 Checkpointer、长期 Store 和 Embedding 的组装。
- `static/`：用于观察对话、工具调用和状态变化的原生前端。
- `test.py`：早期模型直连实验，不作为正式自动化测试入口。
- 维持当前扁平结构，直到出现至少两个边界清晰的领域模块；需要拆目录时，先在 `README.md` 记录新结构及职责，再迁移代码。

## 已接触能力基线

以下状态只表示代码中已有实践，默认均为“已接触、待验证理解”，不得自动写成“已掌握”：

- LLM 调用：通过 OpenAI 兼容接口连接 DeepSeek。
- Agent 工具调用：实现精确查询、语义搜索、长期偏好读写和状态更新工具。
- Agent 状态：使用 `CustomState`、`ToolRuntime` 与 `Command`。
- 短期记忆：使用 `InMemorySaver` 按 `thread_id` 保存消息和状态。
- 长期记忆：使用 `InMemoryStore` 按 `user_id` namespace 隔离跨会话偏好。
- Embedding/检索：支持 DashScope Embedding，并提供本地哈希向量后备方案。
- 应用集成：FastAPI API 与前端能够展示普通消息、工具调用、工具结果和状态摘要。

当前实现的已知边界：

- Checkpointer 和 Store 都是进程内实现，重启后数据丢失，不能代表生产级持久化。
- 本地 `LocalHashEmbeddings` 适合演示接口和检索流程，不是语义模型，不能用来证明真实 Embedding/RAG 效果。
- `model_call_count` 当前实际统计的是 `update_state` 工具更新次数，不是底层模型请求次数。
- `README.md` 和 `.env.example` 声明了 `EMBEDDING_PROVIDER`，但 `store.py` 当前只依据 `DASHSCOPE_API_KEY` 是否存在来选后端；修改 Embedding 配置前先统一这一语义。
- 项目目前没有正式自动化测试、离线评测集、线上评测、Tracing 或质量基线。

## AI 能力缺口与优先级

### P0：先建立可靠的 AI 实验闭环

1. LLM 基础：理解 token、上下文窗口、采样参数、消息角色、模型能力边界、幻觉和非确定性。
2. Prompt 基础：区分 system instruction、用户输入、上下文数据与工具结果；通过对照实验验证提示词，而不是凭主观感觉修改。
3. 结构化输出：使用 Pydantic/JSON Schema 约束模型输出，覆盖解析失败、校验失败和重试策略。
4. 自动化测试：至少覆盖工具纯逻辑、API 参数校验、用户隔离、thread 隔离和失败路径；模型调用使用可控 fake/stub，避免普通单测依赖在线模型。
5. 评测：建立小型固定数据集，分别衡量工具选择、参数正确率、检索命中、回答忠实度、格式正确率和端到端成功率。
6. 可观测性：记录一次请求内的模型调用、工具调用、耗时、token、错误与 trace 关联关系，能够从失败结果定位到具体步骤。

### P1：补齐核心 Agent 能力

1. RAG：实践文档加载、清洗、切分、Embedding、召回、过滤/重排、上下文构造和引用；明确区分“长期记忆”与“知识库检索”。
2. 上下文工程：实践消息裁剪、摘要/压缩、相关记忆选择、token 预算和提示注入隔离。
3. 显式工作流：用原生 LangGraph 构建至少一个含条件路由、可恢复错误和确定性节点的流程，并与 `create_agent` 的抽象进行对比。
4. 流式处理：实现模型 token 或图事件流，理解首 token 延迟、背压、断连和重复事件处理。
5. Human-in-the-loop：对有副作用或高风险的工具增加暂停、审批、拒绝和恢复流程。
6. 可靠性：区分瞬时错误、模型可修复错误、用户输入错误和程序缺陷；设计 timeout、有限重试、幂等与降级策略。

### P2：走向生产与高级能力

1. 安全：处理 prompt injection、越权工具调用、敏感数据泄露、输出校验、最小权限和审计日志。
2. 持久化：将 Checkpointer/Store 迁移到持久后端，验证并发、事务、数据隔离、迁移、恢复和过期策略。
3. 成本与性能：记录 token、模型/Embedding 成本、缓存命中率和端到端延迟，通过数据选择模型和上下文长度。
4. 模型比较：在同一评测集上比较不同模型或参数，不根据单次聊天体验下结论。
5. MCP/外部工具：掌握工具发现、Schema、认证授权、超时、错误映射和不可信工具结果处理。
6. 多 Agent：只有当单 Agent 的职责边界、评测和可观测性已经稳定，且任务确实需要上下文隔离或专业分工时再引入；必须与单 Agent 基线比较质量、延迟和成本。
7. 多模态与微调：作为后续专题；只有明确数据、指标以及 Prompt/RAG 无法解决的问题时才考虑微调。

## 建议学习里程碑

- M1 可测 Agent：为现有工具和 API 建立测试，新增结构化输出，并形成第一份 20 条左右的固定评测集。
- M2 可解释 RAG：接入一组真实文档，展示切分、召回、引用和失败案例，输出 Recall@K/忠实度等指标。
- M3 可控工作流：用 LangGraph 实现显式路由、错误恢复、流式事件与人工审批。
- M4 可运行系统：加入持久化、Tracing、安全边界、并发与成本监控，通过回归评测后再扩展 MCP 或多 Agent。

里程碑必须按证据更新状态，不使用百分比估算。证据包括：可运行代码、自动化测试、固定评测结果、失败案例复盘和本人能解释关键取舍。

## Codex 在本项目中的教学协作规则

- 每次 AI 相关改动前，先说明本次训练的概念、当前假设、最小实验和验收指标；不要只给框架 API 用法。
- 实现时说明框架替用户隐藏了什么。关键概念至少做一次低抽象或原生 SDK/LangGraph 对照，避免只会调用封装。
- 优先给出能暴露机制和失败模式的最小实现，不为展示效果提前引入多 Agent、复杂架构或大量依赖。
- 对模型输出、工具选择、检索结果等概率行为，必须用样本集和指标判断；普通确定性逻辑继续使用单元/集成测试。
- 每个实验至少保留一个失败案例，并记录原因分类：Prompt、模型、检索、工具、状态、数据、安全或基础设施。
- 涉及框架具体 API、模型能力或最佳实践时先查官方最新文档；外部事实不依赖项目记忆。
- 完成一个里程碑后，更新本文件的“已接触能力基线”“已知边界”和里程碑证据；没有验证证据时不得将条目标记为掌握。
- 发现更高优先级缺口时直接指出。默认优先级为：评测与测试 > 正确性与安全 > 可观测性与可靠性 > 新功能 > UI。

## 下一步默认任务

若用户没有指定新的学习主题，优先推进 M1：

1. 先统一 `EMBEDDING_PROVIDER` 的配置语义。
2. 建立正式的 `tests/` 结构与模型 fake/stub 约定；这是新目录，实施前先把结构补充到 `README.md`。
3. 为工具、用户/会话隔离和 API 失败路径补测试。
4. 增加一个结构化输出实验。
5. 建立首版固定评测数据集和可重复执行的评测脚本。

